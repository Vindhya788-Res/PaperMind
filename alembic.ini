# Alembic configuration. The database URL is supplied at runtime from
# application settings (see backend/papermind/db/migrations/env.py), so it is
# intentionally not stored here.

[alembic]
script_location = backend/papermind/db/migrations
prepend_sys_path = backend
path_separator = os
